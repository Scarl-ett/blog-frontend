import React from 'react';
import styledComponents from 'styled-components';

const HeaderBlock = styledComponents.div``;

const Header = () => {
  return (
    <HeaderBlock>

    </HeaderBlock>
  );
};

export default Header;